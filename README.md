# BR30 Founder React Clean

Clean React conversion. Old HTML/CSS files are not kept in the source root. CSS is embedded inside each JSX page using `<style>{`...`}</style>`.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
